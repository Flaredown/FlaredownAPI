# describe "App.EntryRoute", ->
  # describe "responses setup", ->
  #   it "adds blank responses if any are missing", ->
  #     T.controller.get("responses").removeObject(T.controller.get("responses.firstObject"))
  #     
  #     expect(T.controller.get("responses").to.have.length(T.controller.get("questions.length")-1)
  #     
  #     expect(T.controller.responses).to.have.length(T.controller.get("questions.length"))