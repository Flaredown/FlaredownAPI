describe "Using Questioner", ->
  beforeEach ->
    # login, etc
    
  it "does something", ->
    # Em.run ->
    #   T.store.find("question", "1").then(
    #     (res) -> 
    #       debugger
    #   )