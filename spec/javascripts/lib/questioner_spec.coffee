describe "App.AppQuestionerComponent", ->
  
  describe "#sections", ->
    it "checks section login", ->
      # Em.run ->
      #   T.store.find("question", "1").then(
      #     (res) -> 
      #       debugger
      #   )
        
      # expect(App.AppQuestionerComponent)